/*bunch of classes that do one thing and one thing only*/

client is using a facade to communicate with all the other classes

facade simplify the use of all classes

->law of least knowledge

formal definition: The facade pattern provides an unified interface to a 
set of interfaces in a subsystem. Facade defines a higher level interface
that makes the subsystem easier to use